# 校园派送系统：导航接口说明文档

## 核心接口：`getDeliveryRoute`
用于计算：**骑手位置 -> 餐厅 -> 送餐点** 的全程路径。

- **调用方式**：
  `List<String> path = graph.getDeliveryRoute("起点", "餐厅", "终点");`
- **特点**：
  1. **自动去重**：餐厅节点不会在路径中重复出现。
  2. **顺序保证**：返回列表严格按照行驶顺序排列。
  3. **详情关联**：如需显示地点区块（Block），可调用 `graph.getLocationInfo("地点名")`。