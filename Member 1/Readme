第一阶段：设计与实现基础类（代码层面）

创建 Location 类：

虽然只需要名称字段，但建议作为基础对象以便后续扩展。

创建 Edge 类：

包含 destination (目标地点名称) 和 distance (距离/权重) 。

实现 Graph 类：

核心存储结构：必须使用 HashMap<String, List<Edge>> 。
+1

方法实现：

addLocation(String name)：向 Map 中添加新节点 。

addRoute(String from, String to, int distance)：必须是双向的（即 A 到 B 和 B 到 A 都要添加），并验证输入的合法性（距离需 > 0）。

displayMap()：以邻接表的形式清晰展示校园所有地点及其邻居 。

第二阶段：开发最短路径算法（核心逻辑）

实现 Dijkstra 算法：

这是为了处理带权重的路径（即不同路段距离不同） 。
+1

功能要求：

计算两个位置之间的最短总距离 。

记录并返回完整路径序列（例如：骑手位置 → 地点 A → 取餐点） 。

技术点：

使用 PriorityQueue 来优化搜索效率 。

使用一个 Map (如 previousNodes) 来追踪路径，以便最后进行路径重组。

处理特殊情况：如输入的地点不存在或两点之间无法到达。

第三阶段：系统集成与数据准备

为 Member 3 提供接口：

Member 3 (派送系统) 需要调用你的算法来计算派送路径 。

你需要确保你的方法能被外部方便调用，例如：getShortestPath(String start, String end)。

预载地图数据 (与 Member 4 配合)：

在程序启动时，预先建立 5-10 个校园地点（如 Kolej Makanan, Fakulti Sains, Library 等）和它们之间的路线。

第四阶段：文档与演示准备（得分重点）

撰写报告与视频素材：

存储说明：解释为什么选择邻接表和 HashMap（查找效率高） 。

算法演示：准备演示 Dijkstra 算法是如何一步步找到最短路径的 。
+1

复杂度分析：说明算法的时间复杂度（例如：O((V+E)logV)） 。

截图：准备 displayMap() 的运行结果截图放入幻灯片 。